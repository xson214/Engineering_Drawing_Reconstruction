# =====================================================
# Python 3.10 compatible requirements
# =====================================================

# Core OCR — PaddleOCR 2.7.3 + GPU paddle (Python 3.10 compatible)
paddleocr==2.7.3
# paddlepaddle-gpu cài riêng (xem bên dưới)

# PyTorch với CUDA 12.1 (compatible với CUDA 12.7 driver)
# Cài riêng: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Transformers (DETR, TableTransformer)
transformers>=4.35.0,<5.0.0
accelerate>=0.25.0

# Image processing
opencv-python<=4.6.0.66
opencv-contrib-python<=4.6.0.66
pillow>=10.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.11.0

# Data handling
pandas>=2.0.0
json5>=0.9.14

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Geometry
shapely>=2.0.1
pyclipper>=1.3.0

# Web interface
streamlit>=1.28.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.1
requests>=2.31.0
